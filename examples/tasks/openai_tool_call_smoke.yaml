goal: Call fake_tool first, then use its observed result to finish the smoke task.
constraints:
  - This task is for manual OpenAI provider smoke testing.
  - The model must call fake_tool before producing the final answer.
  - Do not use any tool except fake_tool.
allowed_tools:
  - fake_tool
acceptance_criteria:
  - Run reaches completed state.
  - The episode records a fake_tool success in tool-calls.jsonl.
  - Inspect output shows provider=openai and a tool observation for fake_tool.
verification_commands: []
